'use strict';
app.factory('hardwareMapper', [
    '$rootScope',
    'hardwareContext',
    'hardwareConstant',
    function (
        $rootScope,
        hardwareContext,
        hardwareConstant) {

        var serviceFactory = {};
        
        return serviceFactory;

    }]);